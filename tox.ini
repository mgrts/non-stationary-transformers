[flake8]
max-line-length = 99
max-complexity = 10
extend-ignore = E203, W503
# .claude/ holds vendored Claude Code tooling (hooks); not project source.
exclude = .git,__pycache__,docs,build,dist,.venv,venv,.claude
